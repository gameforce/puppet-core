---
  classes:
    - role::default
    #- role::docker

  lookup_options:
    autofs::mounts:
      merge: hash
  autofs::mounts:
    home:
      mount: '/net/data'
      mapfile: '/etc/auto.data'
      options: '--timeout=120'
  autofs::mapfiles:
    home:
      path: '/etc/auto.data'
      mappings:
        - key: '*'
          options: 'rw,soft,intr'
          fs: '192.168.11.10:/share/data'